experiment:
  name: pmxt_research_backtest_v1_smoke
adapter:
  name: pmxt_event_v1
  input:
    # External data dependency (PolyReaper PMXT archive; input hashes are
    # recorded in resolved_config.json/summary.json).  On other machines use
    # either an env var (event_dir: ${POLYREAPER_EVENTS}/highest-temperature-...)
    # or the CLI override: python -m polymarket.backtest_v1 --config ... --event-dir <path>
    event_dir: C:/Projects/PolyReaper/data/curated/polymarket/events/highest-temperature-in-shanghai-on-june-9-2026
    condition_id: "0x715ee73ffee65801adb9f51098cf61a14fb4262ae2b20eeb1d68528bdde59c41"
    asset_id: "93683348120137447419301462197228170067558545967622008841151707439369216600718"
    dataset_id: shanghai-june-9-2026-25c-yes-pmxt-l2
selection:
  asset_id: "93683348120137447419301462197228170067558545967622008841151707439369216600718"
# Explicit opt-in: PMXT research replay contract.  Without this block the
# runner rejects pmxt_event_v1 (default strict_capture mode).
replay:
  mode: pmxt_research
strategy:
  enabled: false
runtime:
  run_id: pmxt-research-smoke-001
report:
  output_dir: ./runs
